#include <iostream>
#include <vector>
namespace misc{
	int countTrueValuesInVector(const std::vector<bool>&);
	std::vector<std::string> pseudoSequence(int);
	void printEncodedSeq(const std::vector<std::string>&);
	bool checkParameters(int, int, int, double, double,bool,double);
};
